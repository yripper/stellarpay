{
  "name": "@stellarpay/hono",
  "version": "0.1.0",
  "description": "One-line Hono middleware adapter for the Stellarpay payment paywall.",
  "keywords": ["stellar", "x402", "mpp", "payments", "paywall", "hono", "middleware"],
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=22" },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prepack": "pnpm build",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@stellarpay/core": "workspace:*"
  },
  "peerDependencies": {
    "hono": ">=4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "hono": "^4"
  }
}
